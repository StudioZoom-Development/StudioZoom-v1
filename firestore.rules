rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    function isSignedIn() { return request.auth != null; }

    // Allow full read and write access for all signed-in team members
    match /{document=**} {
      allow read, write: if isSignedIn();
    }
  }
}
